#ifndef RENDER_SETTINGS_H
#define RENDER_SETTINGS_H

layout(std140, binding=0) uniform RenderSettingsUB
{
	mat3 viewMatrix;
	vec3 eyePosition;
	float time;
	vec4 epAndTime;
	mat3 inverseViewMatrix;
};

#endif
